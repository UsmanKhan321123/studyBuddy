import { Text, View } from "react-native"

export default function ChatsScreen(){

    return(
     <View>
        <Text>This is chat screen which is serving as a index screen for(tabs)</Text>
     </View>
    )
}